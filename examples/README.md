### Examples:
The examples in this directory roughly increase in complexity corresponding to the leading number of the file.

To demonstrate the ease of switching optimizers, some of the `SimulationSettings` have been pulled out
into separate files for re-use.